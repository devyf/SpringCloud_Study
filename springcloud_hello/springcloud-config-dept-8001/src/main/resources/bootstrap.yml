spring:
  cloud:
    config:
      name: config-dept  #需要从github上读取的资源名称，不带.yml后缀名
      profile: test   #访问哪个节点上的信息
      label: master  #读取哪个分支
      uri: http://config3344.com:3344  #本微服务启动后先去找3344 springcloud config配置服务，通过服务连接到github
